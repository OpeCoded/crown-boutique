//this is the action type (in form of string) that is performed by the user, its being exported to the reducer
const CartActionTypes = {
    TOGGLE_CART_HIDDEN:  'TOGGLE_CART_HIDDEN'
}

export default CartActionTypes;